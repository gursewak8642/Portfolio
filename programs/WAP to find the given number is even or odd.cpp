// Write a code to find wether the given number is odd or Even
#include<stdio.h>
#include<conio.h>
int main()
{
	int num;
	printf("\n Enter any random number:" );
	scanf("%d",&num);
	if(num%2==0)
	  printf("\n %d is an even number", num);
	else
	  printf("\n %d is an odd number", num);
	 return 0;   
	
	
	
	
	
}
